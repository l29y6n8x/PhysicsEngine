# Physics Engine

This project uses a Python virtual environment in `.venv` and keeps source files in the project root or `src/` directory.

## Setup

```powershell
cd "c:/Users/linus/OneDrive/Dokumente/VS Code/Physics Engine"
.\.venv\Scripts\Activate.ps1
```

## Project layout

- `.venv/` — virtual environment (do not store project code here)
- `src/` — Python source files
- `README.md` — project notes
